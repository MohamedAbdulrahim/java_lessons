
package lec12v2;

public class Lec12v2 {
   
    public static void main(String[] args) {
        
          /*
    Access Modifires >>>>
        private > call only in his class
        package(default) > call only in the same package like public,out package like private,
                        // if i didn't write any thing is package
        protected > call in the same package or from another any class inherited
        public > call in all classes
        
    Access Modifiers >> used with >> Field , Method , Nested class , class : public or package
    */
// if classA extends classB ,, classA doesn't need to create object from classB to call field , classA call field directly 
    }
    
}
